import { motion } from "framer-motion";

const PageTransition = ({children}) => {
    return (
        <motion.div
            className=""
            initial={{}}
            animate={{}}
            exit={{}}
            transition={{}}
        >
            {children}
        </motion.div>
    )
}

export default PageTransition